# MongoDB Atlas Configuration
# Get your connection string from: https://www.mongodb.com/cloud/atlas
# IMPORTANT: Use the EXACT database name you created in MongoDB Compass (case-sensitive)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/EscapeRoom?retryWrites=true&w=majority
MONGODB_DATABASE=EscapeRoom

# Server Configuration
PORT=4567

# Instructions:
# 1. Copy this file to .env (without .template extension)
# 2. Replace username, password, and cluster with your MongoDB Atlas credentials
# 3. Make sure .env is in your .gitignore (it already is)
# 4. Never commit .env file to version control

